spring:
  datasource:
    # Cấu hình kết nối tới Database nội bộ (trên Server AWS)
    url: jdbc:mysql://localhost:3306/defaultdb
    username: root
    password: "123456"
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect